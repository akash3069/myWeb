apiVersion: v1
kind: Service
metadata:
  labels:
    app: mydeployment
  name: my-service
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  type: NodePort

